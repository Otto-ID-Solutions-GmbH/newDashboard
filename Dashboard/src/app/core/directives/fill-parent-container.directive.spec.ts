import { FillParentContainerDirective } from './fill-parent-container.directive';

describe('FillParentContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FillParentContainerDirective();
    expect(directive).toBeTruthy();
  });
});
