import { ChartMarginsDirective } from './pie-chart-margins.directive';

describe('ChartMarginsDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartMarginsDirective();
    expect(directive).toBeTruthy();
  });
});
