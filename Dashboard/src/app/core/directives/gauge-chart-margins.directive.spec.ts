import { GaugeChartMarginsDirective } from './gauge-chart-margins.directive';

describe('GaugeChartMarginsDirective', () => {
  it('should create an instance', () => {
    const directive = new GaugeChartMarginsDirective();
    expect(directive).toBeTruthy();
  });
});
